name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_run:
    workflows: ["Research Agent"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout v4 source
        uses: actions/checkout@v4
        with:
          path: v4-source

      - name: Checkout pinned v3 source
        uses: actions/checkout@v4
        with:
          ref: 507a1c17c8a5edc1128f0a98e88813fee6413245
          path: v3-source

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: v4-source/package-lock.json

      - name: Install v3 dependencies
        run: npm ci
        working-directory: v3-source

      - name: Build pinned v3
        run: npm run build
        working-directory: v3-source
        env:
          VITE_BASE_PATH: /mosaic/v3/

      - name: Install v4 dependencies
        run: npm ci
        working-directory: v4-source

      - name: Build v4
        run: npm run build
        working-directory: v4-source
        env:
          VITE_BASE_PATH: /mosaic/v4/
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || secrets.VITE_API_BASE_URL || '' }}

      - name: Prepare versioned Pages artifact
        run: |
          rm -rf pages
          mkdir -p pages/v3 pages/v4
          cp -R v3-source/dist/. pages/v3/
          cp -R v4-source/dist/. pages/v4/
          cp v4-source/dist/404.html pages/404.html

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './pages'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
